# Conduit Client agent contract

This public repository contains client-side code only. Never add live credentials, invite secrets, request secrets, private server URLs, workspace roots, or deployment state. Run the Python tests before commits. Keep the SDK source-first, fail closed, and compatible only through explicit versioned contracts; do not add compatibility aliases for unreleased behavior.
