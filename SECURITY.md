# Security

Report vulnerabilities privately to the repository owner. Do not open public issues containing credentials, invitation secrets, request secrets, private server URLs, workspace roots, or live deployment details.

The client fails closed on malformed auth state, unsafe ownership or permissions, unsupported MCP lifecycle, and authorization errors. Direct credential handoff assumes the client source is already present and does not replace invite admission for untrusted agents.
